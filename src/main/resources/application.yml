spring:
  application:
    name: UberSocketServer
  jpa:
    hibernate:
      ddl-auto:

server:
  port: 3002

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    preferIpAddress: true
